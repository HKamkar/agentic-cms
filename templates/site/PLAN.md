# Plan

What is being built, decided and left open, kept current; each finished
phase is rewritten as built. The first entry is the scaffold.

## Scaffold

- `agentic-cms init`: the wireframe example, the agent files, the config.
- Next: `src/config/site.ts` (the brand), `src/app/globals.css` (the
  tokens), `content/VOICE.md` (the voice), the content; then the `design`
  skill.

## Roadmap — open items

- (none yet)
