# Backlog

One line per idea: the working slug in bold, a colon, a one-line brief
(reader, angle, keyword, links). `pnpm content:status` counts the lines
that start with `- `. Promote a line to `calendar.md` with a date when it
is scheduled; delete it when the post exists.
